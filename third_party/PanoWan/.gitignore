# Generated runtime bundle layer (regenerated by `make setup-backends`)
vendor/

# Ephemeral build/setup output
.tmp/
build/

# Upstream-acquired source — not committed to the parent repo because the
# backend.toml [source] entry plus setup tooling are the canonical recipe.
# Anything below this line is upstream-owned and reproducible.
src/
scripts/
LICENSE
README.md
pyproject.toml
uv.lock
.python-version
